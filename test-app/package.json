{
  "name": "gs1-dl-sdk-test-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf ./.cache ./dist",
    "build": "npm run clean && ./node_modules/.bin/parcel build index.html",
    "serve": "npm run clean && ./node_modules/.bin/parcel serve index.html"
  },
  "author": "EVRYTHNG",
  "license": "",
  "dependencies": {
    "evrythng-gs1-dl-sdk": "file:..",
    "hyperapp": "^1.2.9",
    "parcel": "^1.10.3"
  }
}
